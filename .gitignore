node_modules/
artifacts/
cache/
coverage/
coverage.json
typechain-types/
.env

# Claim receipts (vh commit/claim) — contain a secret salt; never commit them.
*.vhclaim.json

# examples/run.js writes its artifacts to an OS temp dir by default; if you point
# VH_EXAMPLE_OUT at a local path under examples/, keep its output out of the tree.
examples/.out/
