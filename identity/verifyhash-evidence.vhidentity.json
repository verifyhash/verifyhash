{"kind":"vh-identity-card-signed","schemaVersion":1,"note":"This is a SIGNED verifyhash identity-card container: it WRAPS (never edits) the EXACT canonical identity-card bytes in `attestation` and attaches a detached EIP-191 signature. verifyIdentityCard RE-DERIVES the signer from those bytes and pins it to the embedded `vendorAddress` — it never trusts the file's own claims. Every caveat of the embedded card applies. This is a verifyhash producer IDENTITY CARD: the holder of `vendorAddress`'s key SIGNED it, binding that address to the `claims` it attests and the `nonClaims` it explicitly does NOT. verify RE-DERIVES the signer from these exact bytes and REQUIRES it to equal `vendorAddress` — it never trusts the file's own claims. It proves IDENTITY + the claim SET ONLY: it does NOT prove any specific sealed/signed packet is true (each packet carries its own proof), it is NOT a trusted TIMESTAMP (\"published since T\" rides the human-owned signing/timestamp trust-root, STRATEGY.md P-3), and it is NOT a legal opinion.","attestation":"{\"kind\":\"vh-identity-card\",\"schemaVersion\":1,\"note\":\"This is a verifyhash producer IDENTITY CARD: the holder of `vendorAddress`'s key SIGNED it, binding that address to the `claims` it attests and the `nonClaims` it explicitly does NOT. verify RE-DERIVES the signer from these exact bytes and REQUIRES it to equal `vendorAddress` — it never trusts the file's own claims. It proves IDENTITY + the claim SET ONLY: it does NOT prove any specific sealed/signed packet is true (each packet carries its own proof), it is NOT a trusted TIMESTAMP (\\\"published since T\\\" rides the human-owned signing/timestamp trust-root, STRATEGY.md P-3), and it is NOT a legal opinion.\",\"vendorAddress\":\"0x7cb4d3dc6c52996b6386473bfb32f898263412f7\",\"productLine\":\"evidence\",\"claims\":[\"Signs verifyhash EVIDENCE seals and evidence licenses: packets whose signer recovers to this vendorAddress were produced by the verifyhash evidence toolchain operated by verifyhash.com\",\"Licenses minted by this key unlock the paid 'vh evidence seal --sign' surface; verify them offline with 'vh trust license verify --vendor 0x7cb4d3DC6C52996B6386473Bfb32f898263412f7'\"],\"nonClaims\":[\"NOT a trusted timestamp: a seal proves tamper-evidence of the bytes, not WHEN they were sealed\",\"NOT a legal opinion or admissibility attestation; the subscription agreement governs paid access\"],\"publishedAt\":\"2026-07-05T03:20:46.337Z\"}\n","signature":{"scheme":"eip191-personal-sign","signer":"0x7cb4d3dc6c52996b6386473bfb32f898263412f7","signature":"0x953d684298679bb70e7bb2e4fbed3713c5f8e997447e7c32b2da2c83d8e2ef772d66703fd949560423992c724bf308b4a2309830b13d78877daf5531e6ea2b611c"}}
